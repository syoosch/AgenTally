<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Thin theme-colored scrollbars, applied app-wide. A transparent 4-DIP
         content-side gutter keeps the visible 8-DIP thumb easy to distinguish
         without reducing its interaction area. The hover state fades a dedicated
         overlay (Opacity only) so shared theme brushes stay untouched. -->

    <ControlTemplate x:Key="ScrollBarThumbTemplate" TargetType="Thumb">
        <Grid>
            <Border Margin="0,1"
                    Background="{DynamicResource ScrollThumbBrush}"
                    CornerRadius="4" />
            <Border x:Name="ThumbHoverOverlay"
                    Margin="0,1"
                    Background="{DynamicResource ScrollThumbHoverBrush}"
                    CornerRadius="4"
                    Opacity="0" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="1" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="0" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="1" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="0" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ScrollBarPageButtonTemplate" TargetType="RepeatButton">
        <Border Background="Transparent" />
    </ControlTemplate>

    <ControlTemplate x:Key="HorizontalScrollBarThumbTemplate" TargetType="Thumb">
        <Grid>
            <Border Margin="1,0"
                    Background="{DynamicResource ScrollThumbBrush}"
                    CornerRadius="4" />
            <Border x:Name="ThumbHoverOverlay"
                    Margin="1,0"
                    Background="{DynamicResource ScrollThumbHoverBrush}"
                    CornerRadius="4"
                    Opacity="0" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="1" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="0" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="1" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation To="0" Duration="0:0:0.12" Storyboard.TargetName="ThumbHoverOverlay" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="LedgerScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Width" Value="12" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="8" />
                        </Grid.ColumnDefinitions>
                        <Track x:Name="PART_Track"
                               Grid.Column="1"
                               IsDirectionReversed="True"
                               Orientation="Vertical">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand"
                                              Focusable="False"
                                              Template="{StaticResource ScrollBarPageButtonTemplate}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Focusable="False"
                                       Template="{StaticResource ScrollBarThumbTemplate}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand"
                                              Focusable="False"
                                              Template="{StaticResource ScrollBarPageButtonTemplate}" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="12" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="{TemplateBinding Background}"
                                  SnapsToDevicePixels="True">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="4" />
                                    <RowDefinition Height="8" />
                                </Grid.RowDefinitions>
                                <Track x:Name="PART_Track"
                                       Grid.Row="1"
                                       Orientation="Horizontal">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageLeftCommand"
                                                      Focusable="False"
                                                      Template="{StaticResource ScrollBarPageButtonTemplate}" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Focusable="False"
                                               Template="{StaticResource HorizontalScrollBarThumbTemplate}" />
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageRightCommand"
                                                      Focusable="False"
                                                      Template="{StaticResource ScrollBarPageButtonTemplate}" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid keeps a named alias for its local resource scope, but all
         scroll areas now share Track-native proportional Thumb sizing. -->
    <Style x:Key="DataGridScrollBarStyle"
           TargetType="ScrollBar"
           BasedOn="{StaticResource LedgerScrollBarStyle}" />

    <Style TargetType="ScrollBar"
           BasedOn="{StaticResource LedgerScrollBarStyle}" />

    <Style x:Key="LedgerScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Margin="{TemplateBinding Padding}"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                Content="{TemplateBinding Content}"
                                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                KeyboardNavigation.DirectionalNavigation="Local"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Row="0"
                                   Grid.Column="1"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   Orientation="Vertical"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Row="1"
                                   Grid.Column="0"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Orientation="Horizontal"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <Border Grid.Row="1"
                                Grid.Column="1"
                                Background="Transparent" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollViewer"
           BasedOn="{StaticResource LedgerScrollViewerStyle}" />

    <!-- ListBox owns its internal ScrollViewer through the framework template.
         Bridge the same canonical scrollbar into that template resource scope
         without replacing the ListBox or ScrollViewer templates. -->
    <Style x:Key="LedgerListBoxStyle" TargetType="ListBox">
        <Style.Resources>
            <Style TargetType="ScrollBar"
                   BasedOn="{StaticResource LedgerScrollBarStyle}" />
        </Style.Resources>
    </Style>

    <!-- Windows theme styles retain a 17-DIP vertical ScrollBar minimum even
         when the canonical style requests 12 DIP. Release that inherited
         minimum only for the statistics master lists so their 8-DIP track can
         reach the same right edge as an unscrolled item. -->
    <Style x:Key="StatisticsListBoxStyle"
           TargetType="ListBox"
           BasedOn="{StaticResource LedgerListBoxStyle}">
        <Style.Resources>
            <Style TargetType="ScrollBar"
                   BasedOn="{StaticResource LedgerScrollBarStyle}">
                <Setter Property="MinWidth" Value="0" />
            </Style>
        </Style.Resources>
    </Style>

    <Style TargetType="ListBox"
           BasedOn="{StaticResource LedgerListBoxStyle}" />
</ResourceDictionary>
